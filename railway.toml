[build]
builder = "NIXPACKS"
buildCommand = "cd backend && mvn clean package -DskipTests && cp target/vedata-workhub-1.0.0.jar ../app.jar"

[deploy]
startCommand = "java -jar app.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3